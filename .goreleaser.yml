# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
version: 2

project_name: mbzr

before:
  hooks:
    - go mod tidy

builds:
  - id: mbzr
    main: .
    binary: mbzr
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.ShortCommit}}
      - -X main.date={{.Date}}

archives:
  - id: default
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - LICENSE
      - README.md

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "Merge pull request"

homebrew_casks:
  - name: mbzr
    repository:
      owner: andpalmier
      name: homebrew-tap
    homepage: "https://github.com/andpalmier/mbzr"
    description: "CLI tool to search in MalwareBazaar database and submit samples"
    directory: Casks
    url:
      verified: github.com/andpalmier/mbzr
    binaries:
      - mbzr
    hooks:
      post:
        install: |
          if OS.mac?
            system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/mbzr"]
          end

docker:
  - image_templates:
      - "ghcr.io/andpalmier/mbzr:{{ .Version }}-amd64"
      - "ghcr.io/andpalmier/mbzr:latest-amd64"
    use: buildx
    goos: linux
    goarch: amd64
    dockerfile: Dockerfile
    build_flag_templates:
      - "--platform=linux/amd64"
    ids:
      - mbzr
    extra_files: []
  - image_templates:
      - "ghcr.io/andpalmier/mbzr:{{ .Version }}-arm64"
      - "ghcr.io/andpalmier/mbzr:latest-arm64"
    use: buildx
    goos: linux
    goarch: arm64
    dockerfile: Dockerfile
    build_flag_templates:
      - "--platform=linux/arm64"
    ids:
      - mbzr
    extra_files: []

docker_manifests:
  - name_template: "ghcr.io/andpalmier/mbzr:{{ .Version }}"
    image_templates:
      - "ghcr.io/andpalmier/mbzr:{{ .Version }}-amd64"
      - "ghcr.io/andpalmier/mbzr:{{ .Version }}-arm64"
  - name_template: "ghcr.io/andpalmier/mbzr:latest"
    image_templates:
      - "ghcr.io/andpalmier/mbzr:latest-amd64"
      - "ghcr.io/andpalmier/mbzr:latest-arm64"

release:
  github:
    owner: andpalmier
    name: mbzr
  draft: false
  prerelease: auto
